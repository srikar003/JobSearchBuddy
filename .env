# JobSearchBuddy environment configuration
# Edit values as needed (no quotes)

# Model used by ResumeWriter (used by `main.py`)
MODEL_NAME=gpt-oss:20b-cloud

# Embedding model used by the embedder (app/core/embeddings.py)
EMBED_MODEL=nomic-embed-text

# Ollama API host
OLLAMA_HOST=http://localhost:11434

# Default request timeout (seconds)
TIMEOUT_S=120
